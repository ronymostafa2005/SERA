# تشغيل الموقع محلياً

1. من جذر المشروع شغّل:
   ```bash
   npm run dev
   ```
2. انتبه للمنفذ الذي يظهر في الطرفية، مثلاً:
   - `Local: http://localhost:3000` أو
   - `Local: http://localhost:3003` (إذا كان 3000 مستخدماً)
3. افتح في المتصفح **نفس الرابط** الذي يظهر (مثلاً http://localhost:3003).

## إذا ظهر 404

- تأكد أن مجلد `app` **غير موجود** في جذر المشروع (يجب أن تكون الصفحات في `src/app` فقط).
- امسح الكاش ثم أعد التشغيل:
  ```bash
  rm -rf .next
  npm run dev
  ```
  (في Windows: احذف مجلد `.next` يدوياً ثم `npm run dev`)

## النشر على Vercel

بعد أي تعديل (مثل حذف مجلد `app`)، احفظ التغييرات وادفع للريبو ثم أعد النشر من لوحة Vercel.
